defmodule Dilee do

	def add(a,b) do
		a+b
	end

	def start() do
		IO.puts "Initializing Dilee"

	end
	def getConsciuos(id,pwd) do

		#call clojure web service to get the consciousness data

	end
	def init(agents) do

	end
end
Dilee.start()
IO.puts "Hello"
IO.puts "surya"


defmodule Dilee.Neuron do
  def add(a,b) do
  	a+b
  end
end
